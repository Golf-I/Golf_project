<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.BoardMapper">


	<!-- 공지사항 전체 게시글 갯수 출력 -->
	<select id="countNotice" resultType="int">
	<![CDATA[
 	SELECT COUNT(*) FROM notice_board
	]]>
	</select>
	<!-- 공지사항 전체 게시글 갯수 출력 -->
	
	
	<!-- 자주묻는질문 전체 게시글 갯수 출력 -->
	<select id="countQnA" resultType="int">
	<![CDATA[
 	SELECT COUNT(*) FROM qna_board
	]]>
	</select>
	<!-- 자주묻는질문 전체 게시글 갯수 출력 -->
	
	
	<!-- 패키지 상품 전체 게시글 갯수 출력 -->
	<select id="countPackage" resultType="int">
	<![CDATA[
 	SELECT COUNT(*) FROM package_product
	]]>
	</select>
	<!-- 패키지 상품 전체 게시글 갯수 출력 -->
	
	
	<!-- 공지사항 전체 게시글 조회 -->
	<select id="selectNotice" resultType="com.spring.domain.BoardVO">
	<![CDATA[
	SELECT *
	FROM notice_board
	WHERE no > 0
	ORDER BY no DESC, date DESC
	LIMIT #{pageStart}, #{perPageNum}
	]]>
	</select>
	<!-- 공지사항 전체 게시글 조회 -->
	
	
	<!-- 자주묻는질문 전체 게시글 조회 -->
	<select id="selectQnA" resultType="com.spring.domain.BoardVO">
	<![CDATA[
	SELECT *
	FROM qna_board
	WHERE no > 0
	ORDER BY no DESC, date DESC
	LIMIT #{pageStart}, #{perPageNum}
	]]>
	</select>
	<!-- 자주묻는질문 전체 게시글 조회 -->
	
	
	<!-- 패키지 상품 전체 게시글 조회 -->
	<select id="selectPackages" resultType="com.spring.domain.ProductVO">
	<![CDATA[
	SELECT *
	FROM package_product
	WHERE package_product_code > 0
	ORDER BY package_product_code DESC
	LIMIT #{pageStart}, #{perPageNum}
	]]>
	</select>
	<!-- 패키지 상품 전체 게시글 조회 -->

	
	<!-- 게시물 조회 -->
	<select id="lookup" resultType="com.spring.domain.BoardVO">
	<![CDATA[
	SELECT *
	FROM notice_board
	WHERE no = #{no}
	AND title = #{title}
	]]>
	</select>
	<!-- 게시물 조회 -->
	
	
	<!-- 상품 게시물 조회 -->
	<select id="oneProduct" resultType="com.spring.domain.ProductVO">
	<![CDATA[
	SELECT A.*, B.*
	FROM package_product A LEFT OUTER JOIN package_product_review B 
	ON A.package_product_code = B.package_product_code
	WHERE A.package_product_code = #{package_product_code}
	]]>
	</select>
	<!-- 상품 게시물 조회 -->
	
</mapper>